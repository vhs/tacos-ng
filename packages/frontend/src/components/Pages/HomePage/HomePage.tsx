import { FC } from 'react'
import Header from '../../Header/Header'

const HomePage: FC = () => {
    return (
        <div>
            <Header text='Dashboard' />
        </div>
    )
}

export default HomePage
